{
  "name": "bind-itempicker-list",
  "version": "3.2.1",
  "dependencies": {
    "reflect-metadata": "^0.1.13"
  },
  "optionalDependencies": {
    "tabris": "^3.3.0",
    "tabris-decorators": "3.4.0"
  },
  "devDependencies": {
    "typescript": "3.3.x"
  },
  "main": "dist/app.js",
  "scripts": {
    "start": "tabris serve -w -a -i",
    "build": "tsc -p .",
    "watch": "tsc -p . -w --preserveWatchOutput --inlineSourceMap",
    "gitpod": "tabris serve -a -w --no-intro --port 8080 --external $(gp url 8080):443"
  }
}
